#!/bin/sh

exec kubectl debug \
    --stdin=true \
    --tty=true \
    --image=docker.io/library/alpine:latest \
    --image-pull-policy=Always \
    --profile=sysadmin \
    "node/${1?error: no node name provided}"
